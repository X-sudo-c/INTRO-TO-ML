from sklearn.tree import DecisionTreeRegressor

#Define model. Specify a number for random state to ensre same results each run

melbourne_model = DecisionTreeRegressor(random_state=1)

melbourne_model.fit(X, y)
from sklearn.tree import DecisionTreeRegressor

#Define model. Specify a number for random state to ensre same results each run

melbourne_model = DecisionTreeRegressor(random_state=1)

melbourne_model.fit(X, y)
